export default defineAppConfig({
  ui: {
    gray: 'neutral',
    primary: 'pp',
    button: {
      rounded: 'rounded-xl',
    },
    notifications: {
      // Show toasts at the top right of the screen
      position: 'top-0 bottom-[unset]'
    }
  },
})
